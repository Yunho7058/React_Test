import axios from 'axios';

const AxiosTest = () => {
  const HandleClick = () => {};
  return (
    <>
      <button onClick={() => HandleClick()}>요청 보내기</button>
    </>
  );
};

export default AxiosTest;
